# AWS Lambda–compatible image for the AI Forecasting Service (Mangum + FastAPI)
FROM public.ecr.aws/lambda/python:3.10

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app ./app

CMD ["app.main.handler"]
