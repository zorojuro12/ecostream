---
description: Python FastAPI Standards
globs: services/ai-forecasting-python/**/*
alwaysApply: true
---
# Python FastAPI Standards
- **Port:** 5000 (Locked for local orchestration).
- **Validation:** All request/response bodies must use Pydantic schemas.
- **Structure:** ML models in `/engine`, API routes in `/api`.
- **Quality:** Use `Ruff` for formatting and `Pytest` for unit tests.